import { FC, memo } from "react";
import { Container } from "@/shared/ui";

const FavoritesPage: FC = memo(() => {
  return (
    <div>
      <Container>Избранное</Container>
    </div>
  );
});

export default FavoritesPage;
